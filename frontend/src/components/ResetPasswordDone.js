import React from 'react';
import './ResetPasswordDone.css';

const ResetPasswordDone = () => {
  return (
    <div className="reset-password-done-container">
      <h1>Password Reset Email Sent</h1>
      <p>Please check your email for instructions on how to reset your password.</p>
    </div>
  );
};

export default ResetPasswordDone;
